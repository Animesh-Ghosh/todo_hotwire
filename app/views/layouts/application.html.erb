<!DOCTYPE html>
<html>
  <head>
    <title>TodoHotwire</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .sort-handle {
        cursor: grab;
        padding: 1rem 1rem 0 1rem;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
